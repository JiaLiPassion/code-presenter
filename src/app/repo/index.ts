import * as js from './js.repo';
import * as zone from './zone.repo';
import * as timeout from './timeout.repo';
import * as zonetimeout from './zonetimeout.repo';
export default {
  js,
  zone,
  timeout,
  zonetimeout
};
